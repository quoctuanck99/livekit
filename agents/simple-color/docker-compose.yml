version: '3'

services:
  worker:
    build: .
    command: tail -f /dev/null #python3 agent.py start
    volumes:
      - ./.env:/opt/app/.env
    env_file:
      - .env
networks:
  mynetwork:
    driver: bridge